# src/telephony/__init__.py

# This __init__.py file marks the 'telephony' directory as a Python package.
# It can be used to export commonly used modules or manage package-level configurations.

# Example: Expose a factory function for telephony clients
# from .sip_client_manager import SIPClientManager
# from .webrtc_client_manager import WebRTCClientManager

# def get_telephony_client(client_type: str, config: dict):
#     if client_type == "sip":
#         return SIPClientManager(config)
#     elif client_type == "webrtc":
#         return WebRTCClientManager(config)
#     else:
#         raise ValueError(f"Unknown telephony client type: {client_type}")

print("✅ Telephony module initialized.")
